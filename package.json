{
	"name": "openui5-fhir-sample",
	"version": "1.0.0",
	"description": "This is a sample UI5 application using the OpenUI5-FHIR library.",
	"license": "SAP SAMPLE CODE LICENSE AGREEMENT",
	"repository": {
		"type": "git",
		"url": "https://github.com/SAP-samples/openui5-fhir-sample-app.git"
	},
	"contributors": [
		{
			"url": "https://github.com/SAP-samples/openui5-fhir-sample-app/graphs/contributors"
		}
	],
	"private": true,
	"engines": {
		"node": ">=10.0.0"
	},
	"devDependencies": {
		"@ui5/cli": "^3.3.0",
		"local-web-server": "^5.2.1"
	},
	"dependencies": {
		"openui5-fhir": "^2.2.8"
	},
	"scripts": {
		"serve": "ui5 serve",
		"serve:dist": "ws --compress -d dist",
		"build": "ui5 build -a --clean-dest",
		"build:self-contained": "ui5 build self-contained -a --clean-dest"
	},
    "files": [
        "webapp/*",
        "ui5.yaml",
        "LICENSES/**",
        ".reuse/**"
    ]
}
